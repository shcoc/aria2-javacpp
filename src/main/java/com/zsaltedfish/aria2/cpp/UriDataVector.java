// Targeted by JavaCPP version 1.5.7: DO NOT EDIT THIS FILE

package com.zsaltedfish.aria2.cpp;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;

import static com.zsaltedfish.aria2.global.Aria2.*;

@Name("std::vector<UriData>") @Properties(inherit = com.zsaltedfish.aria2.prest.Aria2.class)
public class UriDataVector extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public UriDataVector(Pointer p) { super(p); }
    public UriDataVector(UriData value) { this(1); put(0, value); }
    public UriDataVector(UriData ... array) { this(array.length); put(array); }
    public UriDataVector()       { allocate();  }
    public UriDataVector(long n) { allocate(n); }
    private native void allocate();
    private native void allocate(@Cast("size_t") long n);
    public native @Name("operator =") @ByRef UriDataVector put(@ByRef UriDataVector x);

    public boolean empty() { return size() == 0; }
    public native long size();
    public void clear() { resize(0); }
    public native void resize(@Cast("size_t") long n);

    @Index(function = "at") public native @ByRef UriData get(@Cast("size_t") long i);
    public native UriDataVector put(@Cast("size_t") long i, UriData value);

    public native @ByVal Iterator insert(@ByVal Iterator pos, @ByRef UriData value);
    public native @ByVal Iterator erase(@ByVal Iterator pos);
    public native @ByVal Iterator begin();
    public native @ByVal Iterator end();
    @NoOffset @Name("iterator") public static class Iterator extends Pointer {
        public Iterator(Pointer p) { super(p); }
        public Iterator() { }

        public native @Name("operator ++") @ByRef Iterator increment();
        public native @Name("operator ==") boolean equals(@ByRef Iterator it);
        public native @Name("operator *") @ByRef @Const UriData get();
    }

    public UriData[] get() {
        UriData[] array = new UriData[size() < Integer.MAX_VALUE ? (int)size() : Integer.MAX_VALUE];
        for (int i = 0; i < array.length; i++) {
            array[i] = get(i);
        }
        return array;
    }
    @Override public String toString() {
        return java.util.Arrays.toString(get());
    }

    public UriData pop_back() {
        long size = size();
        UriData value = get(size - 1);
        resize(size - 1);
        return value;
    }
    public UriDataVector push_back(UriData value) {
        long size = size();
        resize(size + 1);
        return put(size, value);
    }
    public UriDataVector put(UriData value) {
        if (size() != 1) { resize(1); }
        return put(0, value);
    }
    public UriDataVector put(UriData ... array) {
        if (size() != array.length) { resize(array.length); }
        for (int i = 0; i < array.length; i++) {
            put(i, array[i]);
        }
        return this;
    }
}

